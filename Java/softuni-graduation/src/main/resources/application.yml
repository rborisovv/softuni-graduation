spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/softuni_graduation?useSSL=true&createDatabaseIfNotExist=true
    username: root
    password: root

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

    hibernate:
      ddl-auto: update

  mvc:
    throw-exception-if-no-handler-found: true

  web:
    resources:
      add-mappings: false

jwt:
  secret: 'Y6S*n**={Ov(nWm7k[c^Qhw}c"-y(^^-!#<lXVRfD3^1QpI#^zzmkTfd-23Xfvu'

server:
  port: 8080

logging:
  level:
    org:
      springframework:
        web: DEBUG