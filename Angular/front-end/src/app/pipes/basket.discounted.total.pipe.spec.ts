import { BasketDiscountedTotalPipe } from './basket.discounted.total.pipe';

describe('BasketDiscountedTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new BasketDiscountedTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
